import React from "react";

import classes from "./logo.module.css";

const Logo = (props) => {
  return (
    <div className={classes.Logo}>
      <h4>MovieDB</h4>
    </div>
  );
};

export default Logo;
